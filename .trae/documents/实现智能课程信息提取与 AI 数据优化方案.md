## 1. 基础设施更新
- 为 `Course` 模型增加 `refined_text` 字段用于持久化存储。
- 配置豆包 API 环境。

## 2. 核心逻辑实现
- 编写 `refine_syllabus_with_doubao` 函数，实现高效率的数据脱水。
- 优化 `extract_course_structure` 函数，适配结构化数据输入。

## 3. 工作流闭环
- 在上传视图中集成自动提炼逻辑。
- 在任务生成视图中集成“提炼-读取-绘制”流程。

**我将立即开始部署上述功能。**